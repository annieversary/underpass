# data

### taginfo

can be generated using the following:

```sh
# in the root folder
cargo r --bin update-taginfo
```

### elevation

can be generated using the following:

```sh
cd elevation
./create-dataset.sh
```
